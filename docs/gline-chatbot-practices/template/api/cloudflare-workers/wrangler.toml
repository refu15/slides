# ============================================================
# Cloudflare Workers 設定
#   デプロイ: npx wrangler deploy
#   開発:     npx wrangler dev
# ============================================================

name = "gline-chatbot-api"
main = "worker.ts"
compatibility_date = "2026-04-19"
compatibility_flags = ["nodejs_compat"]

# ------------------------------------------------------------
# 環境変数（本番値は wrangler secret put で設定）
#   wrangler secret put GEMINI_API_KEY
#   wrangler secret put APPLICANT_ENC_KEY
#   wrangler secret put SESSION_SALT
#   wrangler secret put RESEND_API_KEY
# ------------------------------------------------------------
[vars]
ALLOWED_ORIGINS = "https://g-line.co.jp,https://www.g-line.co.jp"
GEMINI_MODEL = "gemini-3.1-flash-lite-preview"
NOTIFY_EMAIL = "info@g-line.co.jp"

# ------------------------------------------------------------
# Hyperdrive binding（AWS RDS 接続プーリング）
#   事前作成: wrangler hyperdrive create gline-pg --connection-string="postgresql://..."
# ------------------------------------------------------------
[[hyperdrive]]
binding = "HYPERDRIVE"
id = "REPLACE_WITH_HYPERDRIVE_ID"

# ------------------------------------------------------------
# Rate Limiting Bindings (Cloudflare 標準、2024 GA)
#   /api/chat  : 10 req/min per IP+session
#   /api/apply : 3  req/min per IP+session
#   /api/gdpr  : 2  req/min per IP（列挙攻撃対策）
# ------------------------------------------------------------
[[unsafe.bindings]]
name = "CHAT_LIMITER"
type = "ratelimit"
namespace_id = "1001"
simple = { limit = 10, period = 60 }

[[unsafe.bindings]]
name = "APPLY_LIMITER"
type = "ratelimit"
namespace_id = "1002"
simple = { limit = 3, period = 60 }

[[unsafe.bindings]]
name = "GDPR_LIMITER"
type = "ratelimit"
namespace_id = "1003"
simple = { limit = 2, period = 60 }

# ------------------------------------------------------------
# Observability
# ------------------------------------------------------------
[observability]
enabled = true

# ------------------------------------------------------------
# Environments
# ------------------------------------------------------------
[env.staging]
name = "gline-chatbot-api-staging"
[env.staging.vars]
ALLOWED_ORIGINS = "https://staging.g-line.co.jp"
GEMINI_MODEL = "gemini-3.1-flash-lite-preview"

[env.production]
name = "gline-chatbot-api-prod"
[env.production.vars]
ALLOWED_ORIGINS = "https://g-line.co.jp,https://www.g-line.co.jp"
GEMINI_MODEL = "gemini-3.1-flash-lite-preview"
