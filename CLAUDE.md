# スライド生成AIエージェント — 厳格ルール

このディレクトリはスライド生成AIエージェントのデザインシステムです。
スライド生成時、以下の3つのルールは**絶対に違反してはならない**。

---

## Rule 1: 原文忠実（Verbatim Fidelity）

素材（議事録、企画書、テキスト）の言葉を**そのまま使う**。言い換え禁止。

- ユーザーが「競争優位性」と書いたら「競争優位性」とスライドに載せる。「強み」に言い換えない。
- 固有名詞、数値、専門用語は一字一句変えない。
- 体言止め・語尾の統一のために語順を入れ替えるのは許可するが、単語そのものの差し替えは禁止。
- 引用元が口語でも、口語のまま載せる。

## Rule 2: パンチライン検出（Punchline Detection）

印象的な結論・比喩・問いかけは、`.slide-punchline` テンプレートで大きく表示する。

- 素材の中に以下のパターンを検出したら、パンチラインスライドとして独立させる:
  - 断定的な結論（「〜である」「〜だ」で終わる強い主張）
  - 比喩・メタファー（「〜のような」「〜こそが」）
  - 問いかけ（「〜ではないか？」「なぜ〜なのか」）
  - 数値インパクト（「ROI 179倍」「3.6億円削減」など）
- パンチラインは原文そのままを `.punch-text` に入れる（Rule 1 との併用）。
- 1つのパンチラインにつき1スライドを使い、前後の文脈スライドとセットで配置する。

## Rule 3: 情報100%カバレッジ（Full Coverage）

削るのではなく、スライドを足す。素材の情報は**1つも落とさない**。

- 入力テキストの全情報がいずれかのスライドに含まれていることを保証する。
- 情報が多い場合は「スライドを分割」する。1枚に詰め込まない。
- 箇条書き7項目以上は2枚に分割する。
- 表の行が6行を超える場合はページを分ける。
- 「要約」「抜粋」「主要なポイントのみ」という判断は禁止。全部載せる。
- 生成後、入力テキストとスライド内容の差分チェックを行い、漏れがないことを確認する。

---

## アーキテクチャ

3層分離:

```
┌─────────────────────────────────────┐
│  デッキ層 (deck)                      │  各プレゼンのHTMLコンテンツ
├─────────────────────────────────────┤
│  テーマ層 (theme)                     │  カラー、フォント、グラデーション
│  css/theme-alno.css                  │
├─────────────────────────────────────┤
│  エンジン層 (engine)                   │  16:9ロック、ナビ、PDF出力
│  css/engine.css + js/engine.js       │
└─────────────────────────────────────┘
```

## ファイル構成

- `css/engine.css` — エンジン層: 16:9ビューポート、ナビゲーション、印刷
- `css/theme-alno.css` — テーマ層: ALNO-AIブランドカラー、フォント、CSS変数
- `css/slides.css` — 17種スライドタイプ
- `css/diagrams.css` — 11種図解パターン
- `js/engine.js` — スライド操作エンジン（スケール、キーボード、概要表示）
- `slide_formats.json` — 全フォーマット定義（30タイプ）

## スライドタイプ一覧 (CSS Class)

| # | Class | 名称 |
|---|---|---|
| 1 | `.slide-title` | タイトル |
| 2 | `.slide-toc` | 目次 |
| 3 | `.slide-divider` | 章扉（セクション区切り） |
| 4 | `.slide-message` | メッセージ／挨拶 |
| 5 | `.slide-punchline` | パンチライン（大文字強調） |
| 6 | `.slide-profile` | プロフィール |
| 7 | `.slide-cards` | カードグリッド（2〜6枚） |
| 8 | `.slide-two-col` | 2カラムテキスト |
| 9 | `.slide-compare` | 比較（Before/After） |
| 10 | `.slide-table` | テーブル |
| 11 | `.slide-kv` | ラベル＋値リスト |
| 12 | `.slide-pain` | 課題提示 |
| 13 | `.slide-metrics` | メトリクスダッシュボード |
| 14 | `.slide-dept` | 部門別詳細 |
| 15 | `.slide-qa` | 質疑応答 |
| 16 | `.slide-thanks` | 感謝 |
| 17 | `.slide-contact` | 連絡先 |

## 図解パターン一覧 (CSS Class)

| # | Class | 名称 |
|---|---|---|
| 1 | `.dia-flow` | プロセスフロー |
| 2 | `.dia-timeline` | タイムライン |
| 3 | `.dia-stepped` | 段階バー |
| 4 | `.dia-pyramid` | ピラミッド／ファネル |
| 5 | `.dia-org` | 組織図 |
| 6 | `.dia-matrix` | マトリクス（2x2） |
| 7 | `.dia-icons` | アイコン行 |
| 8 | `.dia-concentric` | 同心円 |
| 9 | `.dia-compare-arrow` | 比較矢印 |
| 10 | `.dia-gauge-row` | ゲージ／スコアカード |
| 11 | `.dia-cycle` | サイクル図 |

## 操作方法

- `←` `→` `Space` — スライド移動
- `Home` / `End` — 最初/最後へ
- `O` — サムネイル一覧
- `F` — フルスクリーン
- `Ctrl+P` — PDF出力（印刷ダイアログ）
